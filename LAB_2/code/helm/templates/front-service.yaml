apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-service
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-front
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: 80